<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="search">

	<!--지역 정보 불러오기 -->
	<select id="addressProvince" resultType="com.javaex.vo.AddressVo">
	<![CDATA[
	SELECT province
	FROM address
	GROUP by province
	ORDER BY province ASC
	]]>
	</select>
	
	<!--구 정보 불러오기 -->
	<select id="addressCity" parameterType="String"
	resultType="com.javaex.vo.AddressVo">
	<![CDATA[
	SELECT city
	FROM address
	WHERE province = #{province}
	GROUP by city
	ORDER BY city ASC
	]]>
	</select>
	
	<!--동 정보 불러오기 -->
	<select id="addressRegion" parameterType="String"
	resultType="com.javaex.vo.AddressVo">
	<![CDATA[
	SELECT region
	FROM address
	WHERE city like #{city}
	ORDER BY region ASC
	]]>
	</select>
	
	<!--전문분야 정보 불러오기 -->
	<select id="fieldname" resultType="com.javaex.vo.InterestFieldVo">
	<![CDATA[
	SELECT fieldno,fieldname
	FROM interest_field
	]]>
	</select>
	
	
	
</mapper>