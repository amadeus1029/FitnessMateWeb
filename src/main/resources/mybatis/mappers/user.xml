<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<select id="selectSameId" parameterType="String" resultType="int">
		<![CDATA[
		
			SELECT count(userNo)
			FROM users
			WHERE userId = #{id}

		 ]]>
	</select>
	
	<insert id="insertUser" parameterType="com.javaex.vo.UserVo">
		<selectKey keyProperty="userNo" resultType="int" order="BEFORE">
			SELECT seq_userNo.nextval FROM dual
		</selectKey>
		
		<![CDATA[
		
			INSERT INTO users 
			VALUES(#{userNo}, #{userId}, #{password}, #{name}, #{gender}, #{phone}, 0, #{userType}, '#{profileImg}', '', 1, '', '', '')

		 ]]>
	</insert>
	
</mapper>
